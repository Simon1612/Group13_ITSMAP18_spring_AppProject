<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.itsmap.memoryapp.appprojektmemoryapp">

    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION"/>
    <uses-feature android:name="android.hardware.location.network" />
    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".LogIn.SignUpScreen" />
        <activity android:name=".LogIn.ProfileScreen" />
        <activity android:name=".LogIn.ResetPasswordScreen" />
        <activity android:name=".LogIn.LogInScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity" />
        <activity android:name=".Notes.EditNotesActivity" />
        <activity android:name=".MapActivity" />
        <activity android:name=".Notes.ViewNotesActivity" />
        <activity android:name=".Notes.CreateNoteActivity"></activity>
    </application>

</manifest>